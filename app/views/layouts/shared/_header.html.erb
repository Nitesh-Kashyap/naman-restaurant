<nav class="navbar navbar-expand-lg navbar-light" style="background-color: #eae7dc;">
  <div class="container">
    <a class="navbar-brand" href="/">Naman Restaurant</a>
    <% if user_signed_in? %>
    <%= link_to("Edit Profile", edit_user_registration_path, class: "btn btn-info") %>
    <%= link_to("Log Out", destroy_user_session_path, method: :delete, class: "btn btn-danger") %>
    <% else %>
    <%= link_to("Log In", new_user_session_path, class: "btn btn-info sm") %>
    <% end %>
  </div>
</nav>
